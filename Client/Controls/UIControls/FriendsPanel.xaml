<UserControl x:Class="CoinPoker.Views.FriendsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="300">
    <Grid>
        <Border Background="#111" CornerRadius="2,0,0,2">
            <Grid>

                <Grid>
                    <TextBlock Margin="5" Text="XXXX" FontWeight="SemiBold" Foreground="#fff" FontSize="16"/>

                    <!-- panel statusu -->
                    <Border Height="50" VerticalAlignment="Top" Margin="0,35,0,0" CornerRadius="2">
                        <Border.Background>
                            <SolidColorBrush Color="#262626"/>
                        </Border.Background>

                    </Border>
                    <!-- //panel statusu -->

                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
